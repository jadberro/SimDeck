<UserControl x:Class="SimDeck.App.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid Margin="18">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <StackPanel Grid.Row="0" Margin="0,0,0,14">
      <TextBlock Text="Settings" Style="{StaticResource H1}"/>
    </StackPanel>

    <Border Grid.Row="1" Style="{StaticResource CardBorder}" Margin="0,0,0,12">
      <StackPanel>
        <TextBlock Text="Behaviour" FontSize="13" Margin="0,0,0,10"/>

        <CheckBox IsChecked="{Binding LaunchAtStartup}" Foreground="{StaticResource Fg}"
                  Content="Launch SimDeck when Windows starts (minimised to the notification area)"/>

        <CheckBox IsChecked="{Binding CloseToTray}" Foreground="{StaticResource Fg}" Margin="0,10,0,0"
                  Content="Closing the window keeps SimDeck running in the notification area"/>
        <TextBlock Text="{Binding CloseBehaviourHint}" Style="{StaticResource Muted}"
                   Margin="24,4,0,0" TextWrapping="Wrap"/>
      </StackPanel>
    </Border>

    <Border Grid.Row="2" Style="{StaticResource CardBorder}" Margin="0,0,0,12">
      <StackPanel>
        <TextBlock Text="Aircraft profiles" FontSize="13"/>
        <TextBlock Style="{StaticResource Muted}" Margin="0,4,0,10" TextWrapping="Wrap"
                   Text="Profiles map logical names like brake.accum_psi onto the variables the current add-on exposes. Edit the JSON files, then reload."/>
        <Button Content="Reload profiles" HorizontalAlignment="Left"
                Command="{Binding ReloadProfiles}"/>
      </StackPanel>
    </Border>

    <Border Grid.Row="3" Style="{StaticResource CardBorder}">
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Log" FontSize="13" Margin="0,0,0,8"/>
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
          <ItemsControl ItemsSource="{Binding Log}">
            <ItemsControl.ItemTemplate>
              <DataTemplate>
                <TextBlock Text="{Binding}" FontFamily="Consolas" FontSize="11"
                           Foreground="{StaticResource Dim}" Margin="0,1"/>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
        </ScrollViewer>
      </Grid>
    </Border>
  </Grid>
</UserControl>
